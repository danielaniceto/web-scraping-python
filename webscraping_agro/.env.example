# Copie para .env na raiz do projeto. Quando ETL/carga ao Postgres existir,
# adicione python-dotenv em requirements.txt e use load_dotenv() no script.

POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=agro
POSTGRES_USER=postgres
POSTGRES_PASSWORD=postgres
